#!/bin/sh
. ~/.cargo/env
fd \
  --exec-batch \
    rg --no-heading -INor '$1' \
    'url\s+=\s+(?:(?:git@)?github.com:|(?:https|git)://github.com/)([^.]+)' \
    '{}/config' \
  ';' \
  -Htd '^\.git$' \
  "$1" | sort -u
